# SC 预算与 GR 费用申请管理系统需求说明

## 1. 项目定位

本系统是部门内部使用的 SC 预算与 GR 费用申请管理工具。正式 SC 申请、正式 GR 申请、财务审批和扣款仍在外部财务系统中完成。本系统负责记录部门侧的 SC、PO、vendor、GR 流转、金额校验和审计日志。

系统核心对象是 SC，不再把 SC 当作 PO 或其他采购对象处理：

- SC 是预算来源，金额字段为 `sc_amount`。
- PO 是 SC 下的金额分配和扣款对象，金额字段为 `po_amount`。
- vendor 是独立基础资料，通过 PO 与 SC 发生关系。
- GR 必须基于某个 SC 下的某个 PO 发起。

## 2. 已确认硬约束

- 客户端使用 Python + `pywebview`。
- 不建设应用服务端。
- 所有业务处理都在用户客户端完成。
- 数据库为共享文件夹中的 SQLite 文件。
- 读取操作不取应用级业务锁。
- 写入操作使用应用级租约锁，并依赖 SQLite 自身事务保证最终写入一致性。
- 首版不做完整邮件通知系统和复杂自动提醒；但 PO `contract_to` 是需要邮件提醒的业务字段，SC 预估服务期不触发提醒。
- 权限模型是中等防护：客户端做角色控制、业务校验和审计日志；不承诺防止有共享文件夹权限的人绕过客户端直接改库。
- 代码和数据库命名统一使用英文 `vendor`，中文界面可以显示“供应商”。

## 3. 首版目标

首版目标不是极简 MVP，而是可用于部门日常流转的稳定版本。必须包含：

1. 机器 ID 识别用户。
2. 管理员和 requester 两类角色。
3. SC 创建、审批、信息维护、关闭和历史补录。
4. vendor 独立维护，所有授权用户可见。
5. SC 下独立维护 PO；一个 PO 对应一个 vendor。
6. requester 在 SC 通过且 SC No、PO No、vendor 信息完整后发起 GR。
7. 管理员通过 GR，并填写 Con Value。
8. 金额可用性校验，防止 SC amount 或 PO amount 超额。
9. 所有关键操作写入审计日志。
10. 所有授权用户均可查看全部审计日志。
11. 共享 SQLite 并发保护、备份和完整性检查。

首版暂不包含：

- 邮件发送。
- 除 PO `contract_to` 提醒之外的复杂通知策略。
- 预算不足自动提醒。
- 通知任务表和个性化通知策略。
- Excel 导入导出。
- 多级审批、主管角色或复杂工作流。
- 用户名密码登录。
- 应用服务端或共享文件夹请求队列。

## 4. 用户角色

### 4.1 管理员

管理员可以：

- 查看所有 SC、PO、vendor、GR 和日志。
- 代 requester 创建 SC。
- 代 requester 修改 SC 业务信息。
- 审批 SC。
- 关闭 SC。
- 维护任意 vendor。
- 维护任意 SC 下的 PO、PO No、PO amount 和 PO 对应 vendor。
- 代 requester 发起 GR。
- 通过或取消 GR。
- 使用历史补录功能。
- 维护用户、机器 ID、数据库路径、备份和锁状态。

管理员代 requester 操作时，系统必须同时记录：

- 业务归属 requester。
- 实际操作人 admin。
- 操作模式，例如 `normal` 或 `backfill`。

### 4.2 requester

requester 是 SC 的业务负责人，可以：

- 创建自己负责的 SC。
- 修改自己负责 SC 的业务信息，包括 SC No、SC amount、request type、Cost Center、预估服务周期、说明等。
- 维护 vendor 基础资料，所有授权用户可见。
- 在自己负责的 SC 下维护 PO、PO No、PO amount 和 PO 对应 vendor。
- 在满足条件的 SC / PO 下发起 GR。
- 取消自己创建且仍待处理的 GR。
- 查看自己负责的 SC 、PO和 GR。
- 查看全部审计日志。

requester 不应直接修改：

- SC 审批状态。
- GR 的 Con Value。
- 已通过 GR 的历史状态。
- 审计日志。

## 5. 用户识别

每台电脑已有唯一七位机器 ID。系统通过机器 ID 识别当前用户。

建议建立 `users` 表，而不是直接把机器 ID 当作业务用户：

| 字段 | 说明 |
| --- | --- |
| `user_id` | 系统内部用户 ID |
| `machine_id` | 七位机器 ID |
| `user_name` | 用户名称 |
| `role` | `admin` 或 `requester` |
| `email` | 邮箱，首版可只保存不用发送 |
| `status` | 是否启用 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

需要后续确认：

1. 一台电脑是否只对应一个用户。
2. 一个用户是否可能使用多台电脑。
3. 管理员是否可能共用电脑。
4. 用户离职或换电脑时如何处理。

## 6. 核心数据对象

### 6.1 SC

SC 是预算主对象。

`sc_records` 建议字段：

| 字段 | 说明 |
| --- | --- |
| `sc_id` | 系统内部 SC ID |
| `sc_no` | 财务系统中的 SC No；申请 GR 前必须填写 |
| `requester_id` | SC 负责人 |
| `request_type` | SC 类别：`material` / `service` / `fixed_asset` / `FC` |
| `cost_center` | Cost Center，数字，所属部门编号 |
| `sc_amount` | SC amount，即 SC 预算总金额 |
| `service_period_start` | 预估服务开始时间，由原 `start_date` 改名 |
| `service_period_end` | 预估服务结束时间，由原 `end_date` 改名 |
| `status` | SC 状态，直接保存在 SC 表中，不单独建状态表 |
| `description` | SC 说明 |
| `created_by` | 创建人 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |
| `approved_by` | 审批人 |
| `approved_at` | 审批时间 |
| `closed_at` | 关闭时间 |

首版不在 SC 表中保存任何金额汇总字段。SC 的 pending 合计、Con Value 合计、可用金额都按 GR 记录实时计算。

`service_period_start` 和 `service_period_end` 只是预估服务周期，没有强制业务效力，不用于阻断 GR，也不触发邮件提醒。

### 6.2 SC 状态

建议状态：

| 状态 | 含义 |
| --- | --- |
| `pending` | requester 已创建，等待管理员审批 |
| `approved` | 管理员已通过 |
| `denied` | 管理员未通过 |
| `closed` | 已手动关闭 |

常规状态流转：

```text
pending -> approved -> closed
pending -> denied
```

常规业务生命周期：

```text
requester 创建 SC
-> 管理员通过 SC
-> requester 补全 SC No、PO No、PO amount、vendor 信息
-> 可以申请 GR
-> 金额不可用或 SC 手动关闭后生命周期终止
```

历史补录或管理员代办场景下，管理员可以直接创建任意支持状态的 SC，但必须记录审计日志。

SC 是否可以申请 GR 不只取决于状态，还必须同时满足：

1. SC 状态为 `approved`。
2. SC No 已填写。
3. 至少存在一个 PO。
4. 申请所选 PO 的 PO No 已填写。
5. 所选 PO 已关联 vendor。
6. 所选 PO 状态为 `po_approved`。
7. SC 和该 PO 的派生可用金额足够。
8. SC 未关闭。

### 6.3 vendor

vendor 是独立基础资料，由 requester 维护，管理员可以代做，所有授权用户可见。

SC 与 vendor 是多对多关系，通过 PO 关联：

- 一个 SC 可以有多个 PO。
- 一个 PO 只能对应一个 vendor。
- 同一个 vendor 可以出现在多个 SC 或多个 PO 中。

`vendors` 建议字段：

| 字段 | 说明 |
| --- | --- |
| `vendor_id` | vendor ID |
| `vendor_name` | vendor 名称 |
| `ksrm_vendor_code` | KSRM vendor code，可空 |
| `contact_person` | 联系人 |
| `phone` | 电话 |
| `service_scope` | Service scope，选择值 |
| `email` | 邮箱 |
| `description` | 描述 |
| `inquiry_history` | 询价历史，文本即可 |
| `created_by` | 创建人 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

vendor 首版不区分业务状态。

`service_scope` 可选值：

```text
Transportation
engineering Service
Equipment
Parts
Driver
Test car rental
General Service
Dealers
Import&Export&cusoms clearance
Insurance
Harness
Maintenance
Security
Testing support
Others
```

### 6.4 PO

PO 是 SC 下的金额和 vendor 关联对象。

`pos` 建议字段：

| 字段 | 说明 |
| --- | --- |
| `po_id` | 系统内部 PO ID |
| `sc_id` | 所属 SC |
| `vendor_id` | 对应 vendor |
| `po_no` | PO No；申请 GR 前必须填写 |
| `po_amount` | PO amount |
| `status` | PO 状态：`po_pending` / `po_approved` / `finished` |
| `contract_from` | PO 合同开始时间 |
| `contract_to` | PO 合同截止时间，需要后续邮件提醒 |
| `contract_no` | 合同编号，可空 |
| `payment_frequency` | Payment Frequency，文本即可 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

规则：

1. 一个 SC 可以有多个 PO。
2. 一个 PO 必须对应一个 vendor。
3. PO No 可以先为空，但该 PO 在 PO No 填写前不能用于发起 GR。
4. SC 下所有 PO amount 合计不能超过 SC amount。
5. PO amount 修改必须记录修改前后的值。
6. 只有 `po_approved` 且 OPEN PO 足够的 PO 可以用于新建 GR。
7. `finished` PO 不再允许新建 GR。

### 6.5 GR

GR 是 requester 基于某个 SC 和某个 PO 发起的费用申请。

`gr_requests` 建议字段：

| 字段 | 说明 |
| --- | --- |
| `gr_id` | 系统内部 GR ID |
| `po_id` | 对应 PO；所属 SC 通过 PO 反查 |
| `requester_id` | 申请人 |
| `estimated_amount` | 预计费用金额 |
| `con_value` | Con Value，实际 PO 扣除金额 |
| `status` | GR 状态 |
| `remark` | 备注 |
| `created_by` | 实际操作人 |
| `created_at` | 创建时间 |
| `approved_by` | 审批管理员 |
| `approved_at` | 审批时间 |
| `cancelled_by` | 取消人 |
| `cancelled_at` | 取消时间 |

GR 状态：

| 状态 | 含义 |
| --- | --- |
| `pending` | requester 已提交，等待管理员处理 |
| `approved` | 管理员已通过，Con Value 已填写 |
| `cancelled` | 已取消 |

首版 GR 只支持 `pending`、`approved`、`cancelled` 三种状态，不提供管理员退回或驳回状态。管理员如果暂不处理某个 GR，可以让 requester 取消后重新提交，或在备注中说明处理情况。

常规 GR 生命周期：

```text
requester 提交 GR
-> 管理员通过，并填写 Con Value
-> 结束
```

## 7. 金额规则

首版不维护金额汇总冗余字段。数据库只保存：

- SC 的 `sc_amount`。
- PO 的 `po_amount`。
- GR 的 `estimated_amount` 和 `con_value`。

所有展示和校验金额均实时派生。

### 7.1 SC 层派生金额

```text
sc_pending_total = sum(该 SC 下所有 PO 的 pending GR estimated_amount)
sc_con_value_total = sum(该 SC 下所有 PO 的 approved GR con_value)
sc_available_amount = sc_amount - sc_pending_total - sc_con_value_total
allocated_po_amount = sum(该 SC 下所有 PO 的 po_amount)
unallocated_sc_amount = sc_amount - allocated_po_amount
```

说明：

- `sc_available_amount` 用于判断 SC 层是否还能支持新的 GR。
- `allocated_po_amount` 用于判断 PO 分配是否超过 SC amount。
- `unallocated_sc_amount` 表示尚未分配到 PO 的 SC 金额；这部分金额不能直接申请 GR，必须先创建或调整 PO。

### 7.2 PO 层派生金额

```text
po_pending_total = sum(该 PO 下 pending GR 的 estimated_amount)
po_con_value_total = sum(该 PO 下 approved GR 的 con_value)
open_po_amount = po_amount - po_pending_total - po_con_value_total
```

说明：

- `open_po_amount` 是 OPEN PO 的计算结果，不存入 PO 表。
- 创建 GR 时，预计金额必须同时不超过 `sc_available_amount` 和 `open_po_amount`。
- 审批 GR 时，如果 `con_value > estimated_amount`，必须重新校验 SC 和 PO 的派生可用金额。
- 取消 pending GR 后，该 GR 不再计入 pending 合计。

## 8. 主要业务流程

### 8.1 SC 创建

```text
requester 填写 SC 基础信息
-> 系统校验 requester、SC amount、request type、Cost Center 和预估服务周期
-> 系统创建 SC，状态为 pending
-> 记录审计日志
```

创建时必须填写：

- requester。
- SC amount。
- request type。
- Cost Center。
- 预估服务开始时间。
- 预估服务结束时间。

创建时可以暂不填写：

- SC No。
- PO。
- PO No。
- vendor。

### 8.2 SC 审批

```text
管理员查看 pending SC
-> 确认外部财务系统中的 SC 处理结果
-> 管理员通过 SC
-> 系统将 SC 状态改为 approved
-> 记录审批人、审批时间和审计日志
```

SC 通过后，requester 继续补全 SC No、PO、PO No、PO amount 和 vendor。只有信息完整且派生可用金额足够时，才能发起 GR。

### 8.3 vendor 与 PO 维护

```text
requester 维护 vendor 基础资料
-> requester 在自己负责的 SC 下维护 PO
-> 为每个 PO 选择一个 vendor
-> 填写或修改 PO No、PO amount
-> 系统校验 PO amount 合计不超过 SC amount
-> 记录审计日志
```

### 8.4 GR 创建

前置条件：

1. SC 状态为 `approved`。
2. SC No 已填写。
3. 所选 PO 已填写 PO No。
4. 所选 PO 已关联 vendor。
5. 所选 PO 状态为 `po_approved`。
6. SC 和 PO 的派生可用金额足够。

流程：

```text
requester 选择 SC
-> 系统显示该 SC 下可用于申请 GR 的 PO
-> requester 选择 PO
-> 填写预计费用金额和备注
-> 系统获取 SC 业务锁
-> 系统在事务中重新读取最新数据并校验金额
-> 系统创建 GR，状态为 pending
-> 记录审计日志
```

### 8.5 GR 审批通过

```text
管理员查看 pending GR
-> 管理员在外部财务系统完成正式 GR 操作
-> 管理员填写 Con Value
-> 系统获取 SC 业务锁
-> 系统在事务中重新校验金额
-> 系统将 GR 状态改为 approved
-> 记录审批人、审批时间和审计日志
```

### 8.6 GR 取消

- requester 只能取消自己创建且仍为 `pending` 的 GR。
- 管理员可以取消需要处理的 pending GR。
- 取消后该 GR 不再计入 pending 合计。
- 所有取消操作必须记录审计日志。

### 8.7 历史补录

已有历史数据必须兼容。管理员可以在历史补录模式中：

- 创建任意支持状态的 SC。
- 录入任意 SC No、SC amount、request type、Cost Center、预估服务周期和 requester。
- 维护任意 vendor 基础资料。
- 在 SC 下录入任意 PO、PO No、PO amount 和 PO 对应 vendor。
- 录入任意支持状态的 GR。
- 对 approved GR 录入 Con Value。

历史补录必须记录 `operation_mode = backfill` 的审计日志。

## 9. 权限与日志

权限应同时在前端入口和 Python API 层校验。前端隐藏按钮只用于体验，不能作为唯一权限控制。

日志规则：

- 所有关键写操作必须写审计日志。
- 所有授权用户均可查看全部日志。
- 日志只读，普通业务流程不得修改日志。
- 日志记录实际操作人、机器 ID、业务归属 requester、操作前后数据和时间。

建议记录的操作：

- 创建、修改、审批、关闭 SC。
- 新增、修改 vendor。
- 新增、修改 PO。
- 修改 PO amount。
- 创建、取消、审批通过 GR。
- 修改 Con Value。
- 历史补录。
- 用户权限变更。
- 备份、完整性检查、锁清理。

## 10. 共享 SQLite 并发设计

SQLite 自带写入锁，但共享文件夹中的锁可靠性取决于底层网络文件系统。系统不能只依赖 SQLite，需要增加应用级租约锁。

### 10.1 锁层级

```text
system
  用于用户管理、设置、数据库迁移、恢复、全库维护。

sc:{sc_id}
  用于某个 SC 下的 SC 信息、PO、GR 和金额校验。

db-write-gate
  极短时间锁，只包住 SQLite 写事务入口到提交。
```

读取规则：

- 读取不取应用级业务锁。
- SC 列表、SC 详情、GR 列表、vendor 列表和日志查询直接读 SQLite。
- UI 展示过的数据不能直接作为写入依据。写操作进入事务后必须重新读取最新状态并再次校验。

写入规则：

```text
获取业务租约锁，例如 sc:{sc_id}
-> 获取 db-write-gate 租约锁
-> 打开 SQLite 连接
-> BEGIN IMMEDIATE
-> 重新读取最新状态并校验
-> 写入业务变化
-> 写入审计日志
-> COMMIT / ROLLBACK
-> 释放 db-write-gate
-> 释放业务租约锁
```

租约锁文件应包含：

- owner machine ID。
- user。
- process ID。
- operation。
- token。
- `acquired_at`。
- `heartbeat_at`。
- `expires_at`。

如果用户拿到锁后断网，heartbeat 会停止。其他客户端发现锁超过过期时间后，可将其识别为 stale lock。管理员可以在系统工具中查看和清理 stale lock，避免永久卡住。

SQLite 连接策略：

- 使用 rollback journal，不使用 WAL。
- 设置 `busy_timeout`。
- 写事务必须短。
- 数据库繁忙或锁失败时给出明确错误，不静默重复提交。

## 11. 页面结构

系统以 SC 为中心。vendor 和 GR 依赖 SC 展示，不作为首版一级主工作区。

一级导航建议：

```text
首页
SC
待办
日志
系统
```

### 11.1 首页

管理员看到：

- pending GR 数量。
- pending SC 数量。
- 已通过但信息未补全、暂不能申请 GR 的 SC 数量。
- 锁和数据库异常提示。
- 近期 SC。
- 最近审计日志。

requester 看到：

- 自己负责的 SC。
- 自己已通过但待补全 SC No / PO No / vendor 信息的 SC。
- 自己的 pending GR。
- 最近 GR 处理结果。

### 11.2 SC

SC 是主工作区：

- SC 列表。
- SC 详情。
- 新建 SC。
- 历史补录。

SC 详情页建议分区：

```text
基本信息
PO 与 vendor
GR 记录
金额
日志
```

### 11.3 待办

- 管理员：pending SC、pending GR。
- requester：自己的 pending GR、需要补全信息或即将不可用的 SC。

### 11.4 日志

- 所有授权用户可查看全部日志。
- 日志只读。
- 支持按操作人、对象类型、对象 ID、时间范围筛选。

### 11.5 系统

管理员可用：

- 用户与机器 ID 管理。
- 数据库路径和状态。
- 锁状态。
- stale lock 清理。
- 手动备份。
- 完整性检查。
- 基础设置。

requester 默认不显示系统页；如有需要，只展示只读数据库状态。

## 12. 查询、搜索与排序

系统必须支持按文本、字段条件进行搜索和筛选，并支持按字段排序。搜索能力覆盖 SC、vendor、PO、GR 和日志。

### 12.1 通用规则

- 每个列表页至少支持一个全局文本搜索框。
- 每个核心字段应尽量支持字段筛选。
- 日期、金额、状态、枚举字段应支持结构化筛选。
- 列表表头应支持升序 / 降序排序。
- 组合条件采用 AND 逻辑。
- 搜索和排序只读取数据库，不取应用级业务锁。
- 大列表应分页或限制单次返回数量，避免一次性加载过多数据。

### 12.2 SC 搜索与排序

SC 列表支持按以下文本搜索：

- SC No。
- requester 名称。
- request type。
- Cost Center。
- 描述。
- vendor 名称。
- PO No。

SC 列表支持按以下字段筛选：

- requester。
- SC 状态。
- request type。
- Cost Center。
- service period start / end 范围。
- SC amount 范围。
- 是否已填写 SC No。
- 是否存在可用于申请 GR 的 PO。

SC 列表支持按以下字段排序：

- SC No。
- SC amount。
- request type。
- Cost Center。
- service period start。
- service period end。
- status。
- created_at。
- updated_at。

### 12.3 vendor 搜索与排序

vendor 列表支持按以下文本搜索：

- vendor name。
- KSRM vendor code。
- contact person。
- phone。
- email。
- service scope。
- description。
- inquiry history。

vendor 列表支持按以下字段筛选：

- service scope。
- 是否有关联 PO。
- created_by。
- created_at 范围。

vendor 列表支持按以下字段排序：

- vendor name。
- KSRM vendor code。
- service scope。
- created_at。
- updated_at。

### 12.4 PO 搜索与排序

PO 列表和 SC 详情下的 PO 分区支持按以下文本搜索：

- PO No。
- contract no。
- vendor name。
- KSRM vendor code。
- payment frequency。
- SC No。

PO 支持按以下字段筛选：

- 所属 SC。
- vendor。
- PO 状态。
- contract_from 范围。
- contract_to 范围。
- PO amount 范围。
- OPEN PO 范围。
- 是否缺少 PO No。
- 是否临近 contract_to。

PO 支持按以下字段排序：

- PO No。
- vendor name。
- PO amount。
- OPEN PO。
- PO 状态。
- contract_from。
- contract_to。
- created_at。
- updated_at。

### 12.5 GR 搜索与排序

GR 列表和 SC 详情下的 GR 分区支持按以下文本搜索：

- GR ID。
- SC No。
- PO No。
- vendor name。
- requester 名称。
- remark。

GR 支持按以下字段筛选：

- GR 状态。
- requester。
- 所属 SC。
- PO。
- vendor。
- estimated amount 范围。
- Con Value 范围。
- created_at 范围。
- approved_at 范围。

GR 支持按以下字段排序：

- GR ID。
- estimated amount。
- Con Value。
- status。
- created_at。
- approved_at。
- requester。

### 12.6 日志搜索与排序

日志支持按以下条件筛选：

- 操作人。
- 机器 ID。
- 操作对象类型。
- 操作对象 ID。
- action type。
- SC。
- 时间范围。
- operation mode。

日志支持按时间、操作人、对象类型、action type 排序。

## 13. 开发阶段

### 阶段 1：项目骨架与数据库基础

目标：应用能启动，能连接共享 SQLite，有迁移和基础表。

内容：

- 建立 Python 项目结构。
- 集成 `pywebview`。
- 建立前端静态页面结构。
- 配置共享数据库路径。
- 初始化 SQLite schema。
- 实现 `schema_migrations`。
- 设置 SQLite 连接参数。
- 建立基础错误处理。

验收：

- 首次启动能创建数据库。
- 再次启动能识别数据库版本。
- 数据库路径不可用时有明确提示。

### 阶段 2：身份、权限、锁、日志

目标：多用户共享数据库的基础可靠性可用。

内容：

- 机器 ID 识别。
- 用户表与角色判断。
- 未授权设备页面。
- 管理员维护用户。
- 租约锁：`system`、`sc:{id}`、`db-write-gate`。
- stale lock 检测和清理。
- 审计日志基础能力。
- 数据库完整性检查。
- 手动备份。

验收：

- 不同机器 ID 能识别不同用户。
- 未授权用户无法进入业务页面。
- 写操作能生成日志。
- 模拟残留锁后可识别和清理。

### 阶段 3：SC 主流程

目标：SC 作为主工作区可用。

内容：

- 首页和 SC 列表。
- requester 新建 SC，管理员可代建。
- SC 详情基本信息。
- requester 维护 SC 业务信息，管理员可代维护。
- 管理员审批 SC 并执行状态变更。
- SC 关闭。
- SC 权限：管理员看全部，requester 只看自己的 SC。
- SC 详情页展示相关日志。

验收：

- requester 可创建和维护自己负责的 SC，管理员可代做。
- 管理员可通过或不通过 SC。
- requester 只能修改自己负责的 SC。
- SC 状态和基础校验正确。

### 阶段 4：vendor、PO 与金额校验

目标：vendor 基础资料可复用，SC 下 PO / PO amount 可维护，派生金额校验稳定。

内容：

- vendor 基础资料维护页，所有授权用户可见。
- vendor 字段包括 KSRM vendor code、联系人、电话、service scope、邮箱、描述、询价历史。
- SC 详情下的 PO 分区。
- 新增、编辑 PO，并为每个 PO 选择一个 vendor，维护 PO 状态、合同起止时间、合同编号和付款频率。
- 校验 PO amount 合计不超过 SC amount。
- 金额引擎按 GR 记录计算 pending、Con Value、SC available 和 OPEN PO 派生金额。

验收：

- vendor 可被多个 SC / PO 复用。
- SC 下可维护多个 PO，每个 PO 对应一个 vendor。
- SC 未填写 SC No、所选 PO 未填写 PO No 或 PO 状态不是 `po_approved` 时，不能发起 GR。
- 派生金额计算正确，数据库中不保存金额汇总冗余字段。

### 阶段 5：GR 流程与历史补录

目标：费用申请闭环可用，历史数据可兼容。

内容：

- SC 详情下的 GR 分区。
- requester 发起 GR。
- requester 取消 pending GR。
- 管理员审批通过并录入 Con Value。
- 待办页：pending GR / pending SC。
- 历史补录模式：任意支持状态的 SC、PO、vendor、GR，并自动计算派生金额。

验收：

- GR 创建、取消、通过后的派生金额变化正确。
- `con_value > estimated_amount` 时会重新校验派生可用金额。
- 历史补录后派生金额计算正确。

### 阶段 6：查询、打磨、打包

目标：达到部门内部可交付使用。

内容：

- SC、vendor、PO、GR、日志的文本搜索、字段筛选和字段排序。
- SC 内搜索：SC No、vendor、PO No、GR。
- 错误提示和确认弹窗。
- 表格分页、筛选、排序和默认排序规则。
- 数据库备份策略完善。
- 打包为 Windows 可执行程序。
- 编写使用说明和管理员维护说明。
- 基础测试数据和回归测试脚本。

验收：

- 非技术用户能按说明完成日常操作。
- 管理员能维护用户和备份数据库。
- 常见异常有清晰提示。
- 打包程序能在目标 Windows 环境运行。

## 14. 后续扩展

以下能力不进入首版，但可保留扩展空间：

- 邮件通知。
- PO 合同截止时间提醒。
- SC / PO 金额提醒。
- pending GR 超时提醒。
- Excel 导入导出。
- 更强权限模型。
- 多级审批。
- 报表快照。

邮件通知如后续开发，建议作为独立通知程序读取业务数据和通知任务，不放进每个客户端直接发送。
