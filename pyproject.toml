[project]
name = "sc-gr-management"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pywebview>=6.2.1",
    "pystray>=0.19.5",
    "pillow>=11.0.0",
    "pywin32>=306",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
